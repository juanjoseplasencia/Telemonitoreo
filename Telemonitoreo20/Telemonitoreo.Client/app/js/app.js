'use strict';

var telemonitoreoApp = angular.module('telemonitoreoApp',[]);

/*
var telemonitoreoApp = angular.module('telemonitoreoApp', ['ngRoute'])
.config(function($routeProvider, $locationProvider) {
    $routeProvider.when('/Gestantes');
    $locationProvider.html5Mode(true);
});
*/